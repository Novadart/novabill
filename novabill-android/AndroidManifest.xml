<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.novadart.novabill.android"
    android:versionCode="2003000"
    android:versionName="2.3-SNAPSHOT" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SYNC_STATS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name="com.novadart.novabill.android.DispatcherActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.novadart.novabill.android.HomeActivity" >
        </activity>

        <service android:name="com.novadart.novabill.android.ManagerService" />

        <receiver
            android:name="com.novadart.novabill.android.StartManagerServiceReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <service android:name="com.novadart.novabill.android.authentication.NovabillAuthenticatorService" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <activity android:name="com.novadart.novabill.android.authentication.LoginActivity" >
        </activity>
        <activity
            android:name="com.novadart.novabill.android.SelectAccountActivity"
            android:label="@string/title_activity_select_account" >
        </activity>

        <provider
            android:name="com.novadart.novabill.android.content.provider.NovabillContentProvider"
            android:authorities="com.novadart.novabill"
            android:exported="true"
            android:grantUriPermissions="true"
            android:label="NovabillProvider" />

        <service
            android:name="com.novadart.novabill.android.sync.NovabillSyncService"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>
            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/sync_adapter" />
        </service>
    </application>

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="com.novadart.novabill.android" >
    </instrumentation>

</manifest>